@{
    ViewBag.Title = "TRMS - Payroll Generate";
    Layout = "~/Areas/HR/Views/Shared/_Layout.cshtml";
}

<style>
    .monthPicker {
        z-index: 1000;
    }
</style>

<!-- Start content -->
<div class="container-fluid">
    <!-- Page-Title -->
    <div class="row">
        <div class="col-sm-12">
            <div class="page-title-box">
                <h4 class="page-title">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblWelcome") <span class="text-danger">'@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("menu.HR")'</span>! [@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblLoginID"): <span class="text-warning" title="@MvcApplication1.ViewModel.GlobalVariables.CheckNULLValidation(MvcApplication1.ViewModel.GlobalVariables.GV_EmployeeId)">@User.Identity.Name</span> & @MvcApplication1.ViewModel.GlobalVariables.GetStringResource("dashboard.Name"): <span class="text-success">@MvcApplication1.ViewModel.GlobalVariables.CheckNULLValidation(MvcApplication1.ViewModel.GlobalVariables.GV_EmployeeName)</span>]</h4>
                <ol class="breadcrumb float-right">
                    <li class="breadcrumb-item"><a href="/HR">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("menu.HR")</a></li>
                    <li class="breadcrumb-item active">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("menu.Reports")</li>
                </ol>
                <div class="clearfix"></div>
            </div>
        </div>
    </div>

    <!-- Page-Content -->
    <div class="row">
        <div class="col-12">
            <div class="card-box">
                <!-- <div class="panel-heading">
                    <h4>Invoice</h4>
                </div> -->
                <div class="panel-body">
                    <div class="clearfix">
                        <div class="pull-left">
                            <h3 class="text-right"><i class="mdi mdi-desktop-classic"></i> @MvcApplication1.ViewModel.GlobalVariables.GetStringResource("title.payrollmanagement") </h3>
                        </div>
                        <div class="pull-right small text-muted">
                            @DateTime.Now.ToString("f")
                        </div>
                    </div>

                    <hr>

                    <div class="row">


                        <div class="col-4">
                            <h4>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("title.searchbydepartment")</h4>
                            @using (Html.BeginForm("PayrollReportByMonthDepartmentDataHandler", "Reports", FormMethod.Post, new { id = "payroll-department-form" }))
                            {

                                <div class="row">
                                    <div class="col-12">
                                        <label class="control-label">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblSelectDepartment")</label>
                                        <select id="department_id" name="department_id" class="form-control">
                                            @{
                                                if (ViewBag.DepartmentsList != null)
                                                {
                                                    <option value="">-- Select Department --</option>

                                                    foreach (var e in ViewBag.DepartmentsList as List<ViewModels.Department>)
                                                    {
                                                        <option value="@e.id">@e.name</option>
                                                    }
                                                }
                                                else
                                                {
                                                    <option value="">-- NO Department Found --</option>
                                                }
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="row m-t-10">
                                    <div class="col-12">
                                        <label class="control-label">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblSelectPayrollMonth")</label>
                                        <input id="month_year" type="text" name="month_year" class="form-control monthPicker" autocomplete="off" required />
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-12 text-danger">
                                        @Html.AntiForgeryToken()
                                        @Html.ValidationSummary()
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-12">
                                        <button type="submit" id="search-payroll-report" onclick="clearr();" class="btn btn-primary waves-effect waves-light m-t-10">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("btnSearch")</button>
                                        <button type="button" class="btn btn-primary waves-effect waves-light m-t-10" onclick="redirectToPage();">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("btnReset")</button>
                                    </div>
                                </div>
                                                }
                        </div>

                        <div class="col-3">
                            <h4>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("title.generateNewPayroll")</h4>
                            @{
                                string cssDisable = "", cssClass = "";

                                if (ViewBag.GeneratePayrollStatus != null && ViewBag.GeneratePayrollStatus.ToString() == "disabled")
                                {
                                    cssDisable = "disabled"; cssClass = "btn-dark";
                                }
                                else
                                {
                                    cssDisable = ""; cssClass = "btn-primary";
                                }

                            }
                            @using (Html.BeginForm("PayrollGenerate", "Reports", FormMethod.Post, new { id = "generate-payroll-form" }))
                            {
                                <button type="submit" id="generate-payroll" @cssDisable class="btn @cssClass waves-effect waves-light m-t-10">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("btnGeneratePayroll")</button>
                            }
                        </div>



                        <div class="col-5">
                            <h4>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("title.updatePayrollStatus")</h4>
                            <h3 class="text-center text-danger">@ViewBag.PayrollUpdateStatus</h3>
                            @using (Html.BeginForm("PayrollStatusUpdate", "Reports", FormMethod.Post, new { id = "payroll-status-form" }))
                            {
                                <div class="row">
                                    <div class="col-12">
                                        <label class="control-label">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblSelectDepartment")</label>
                                        <select id="sdepartment_id" name="sdepartment_id" class="form-control">
                                            @{
                                                if (ViewBag.DepartmentsList != null)
                                                {
                                                    <option value="">-- Select Department --</option>

                                                    foreach (var e in ViewBag.DepartmentsList as List<ViewModels.Department>)
                                                    {
                                                        <option value="@e.id">@e.name</option>
                                                    }
                                                }
                                                else
                                                {
                                                    <option value="">-- NO Department Found --</option>
                                                }
                                            }
                                        </select>
                                    </div>
                                </div>

                                <div class="row m-t-10">
                                    <div class="col-12">
                                        <label class="control-label">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblSelectPayrollMonth")</label>
                                        <input id="smonth_year" type="text" name="smonth_year" class="form-control monthPicker" autocomplete="off" required />
                                    </div>
                                </div>

                                <div class="row m-t-10">
                                    <div class="col-12">
                                        <label class="control-label">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblPaymentDate")</label>
                                        <input id="spayment_date" type="text" name="spayment_date" class="form-control datepicker" autocomplete="off" required />
                                    </div>
                                </div>

                                <div class="row m-t-10">
                                    <div class="col-12">
                                        <label class="control-label">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblSetStatus")</label>
                                        <select id="spayment_status_id" name="spayment_status_id" class="form-control">
                                            <option value="1">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblUnpaid")</option>
                                            <option value="2" selected>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblPaid")</option>
                                        </select>
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-12 text-danger">
                                        @Html.AntiForgeryToken()
                                        @Html.ValidationSummary()
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-12">
                                        <button type="submit" id="search-payroll-report" onclick="clearr();" class="btn btn-primary waves-effect waves-light m-t-10">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("btnUpdateNow")</button>
                                    </div>
                                </div>
                                                }
                        </div>

                    </div>

                  

                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="card-box">
                <!-- <div class="panel-heading">
                    <h4>Invoice</h4>
                </div> -->
                <div class="panel-body">
                    <div class="row">
                        <div class="col-12">
                            <table id="payroll_report_table" class="table table-striped table-bordered dataTable dt-responsive nowrap no-footer small" style="width: 100%;">
                                <thead>
                                    <tr>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblID")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblSalaryMonthYear")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("monthly.epcode")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblFullName")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblGrossSalary")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblDeduction")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblNetSalary")</th>
                                        <th>@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblAction")</th>
                                        <th>CNIC #</th>
                                        <th>Basic Pay (Rs.)</th>
                                        <th>Increment (Rs.)</th>
                                        <th>Transport (Rs.)</th>
                                        <th>Mobile (Rs.)</th>
                                        <th>Medical (Rs.)</th>
                                        <th>Cash Allowance (Rs.)</th>
                                        <th>Commission (Rs.)</th>
                                        <th>Overtime (in hours)</th>
                                        <th>Overtime Amount (Rs.)</th>
                                        <th>Food (Rs.)</th>
                                        <th>Night (Rs.)</th>
                                        <th>Rent (Rs.)</th>
                                        <th>Group Allowance (Rs.)</th>
                                        <th>Yearly Bonus (Rs.)</th>
                                        <th>Leaves Count (yearly remaining)</th>
                                        <th>Leaves Encash (yearly remaining) (Rs.)</th>
                                        <th>Income Tax (Rs.)</th>
                                        <th>Fine/Extra Amount (Rs.)</th>
                                        <th>Mobile Deduction (Rs.)</th>
                                        <th>Absents Count (Last Month)</th>
                                        <th>Absents Amount (Last Month) (Rs.)</th>
                                        <th>EOBI Employee (Rs.)</th>
                                        <th>EOBI Employer (Rs.)</th>
                                        <th>SESSI Employee (Rs.)</th>
                                        <th>SESSI Employer (Rs.)</th>
                                        <th>Loan Installment (Rs.)</th>
                                        <th>Other Deduction (Rs.)</th>
                                        <th>Payment Mode</th>
                                        <th>Bank Name</th>
                                        <th>Bank Account Title</th>
                                        <th>Bank Account Number</th>
                                        <th>NTN #</th>
                                        <th>EOBI #</th>
                                        <th>SESSI #</th>
                                        <th>Ack Status</th>
                                        <th>Pay Status</th>
                                        <th>Comments</th>
                                        <th>Attachment Path</th>
                                        <th>Is First Month?</th>
                                        <th>Payment Date</th>
                                        <th>Create Date</th>
                                        <th>Update Date</th>
                                    </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                    </div>

                    <div class="row m-t-10">
                        <div class="col-12">
                            <button type="submit" id="btnExport" onclick="ExortExcleBar();" class="btn btn-primary waves-effect waves-light">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("btnExportGridData")</button>
                            <span id="demo" class="text-danger"></span>
                            <p id="export_wait2" style="display: none;" class="text-danger"><img src='/images/loader.gif' alt='processing' /></p>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

</div>
<!-- End content -->
<!-- Custom Modal -->
<div class="modal fade bs-example-modal-sm" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel" aria-hidden="true" style="display: none;">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title" id="mySmallModalLabel">@MvcApplication1.ViewModel.GlobalVariables.GetStringResource("lblMessageAlert")</h4>
            </div>
            <div class="modal-body">
                <span id="alert_message"></span>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div><!-- /.modal -->

@section JS {

    @*<script type="text/javascript" src="@Href("~/Scripts/chosen.ajaxaddition.jquery.js")"></script>*@
    @*<script type="text/javascript" src="@Href("~/Scripts/GenerateReports.js")"></script>*@
    <script type="text/javascript" src="@Href("~/Scripts/monthpicker.js")"></script>


    <script>

        // -------------------- added by IR ---------------------

        //$("#btnShow").click(function () {
        //    if ($("#from").val().length == 0 || $("#to").val().length == 0) {
        //        alert('All fields are required');
        //    }
        //    else {
        //        alert('Selected Month Range :' + $("#from").val() + ' to ' + $("#to").val());
        //    }
        //});

        function redirectToPage() {
            window.location = '/HR/Reports/PayrollGenerate';
        }

        function generatePayrollStatement() {

            if ($('#salary_month_year').val() == "" || $('#salary_month_year').val() == null) {

                $('#alert_message').text("Please select a Month.");
                $('.bs-example-modal-sm').modal('show');

                //alert('Please select a Month.');
                return false;
            }

            $('#pdf-no-data-found').val("");
            $('#pdf-no-data-found').text("");

            $('#export_wait').show();
            setTimeout(function () { $('#export_wait').hide(); }, 5000);

            $('#month_year').val($('#salary_month_year').val());

            //myFunction();
        }

        $("#payroll-report-form").submit(function (e) {
            document.getElementById("generate-payroll-report").disabled = true;
            return true;
        });

        function clearr() {
            $('input[type="search"]').focus();
            $('input[type="search"]').val("");

            //if ($('#monthly-time-sheet-employees').val() != null) {
            //    $('#payroll_report_table').DataTable().search("");
            //    $('#payroll_report_table').DataTable().draw();
            //}

            $('input[type="search"]').blur();

        }

        function HideMessage() { setTimeout(function () { $('#pdf-no-data-found').hide(); }, 5000); }


        function myFunction() {
            setTimeout(function () { $('#export_wait').hide(); document.getElementById("generate-payroll-report").disabled = false; }, 1500);
        }

        function ExortExcleBar() {
            $('#export_wait2').show();
            document.getElementById("btnExport").disabled = true;
            HideBarExcle();
            var ss = $("tr").children("td:first").text();
            if (ss == "No data available in table" || ss == "" || ss == " ") { document.getElementById("demo").innerHTML = "No Data Found."; }
            else { DownloadExcleCsv(); }
        }

        function DownloadExcleCsv() { setupTableExportButton('#payroll_report_table', 'PayrollReport'); }

        function HideBarExcle() {
            setTimeout(function () { $('#export_wait2').hide(); document.getElementById("btnExport").disabled = false; document.getElementById("demo").innerHTML = ""; }, 1500);
        }

        function submitcheck() {

            //var monthYear = $('[name="salary_month_year"]').val();

            //console.log('222 ' + monthYear);

            //if (monthYear == "") {

            //    $('#alert_message').text("Please select a Month.");
            //    $('.bs-example-modal-sm').modal('show');

            //    //alert("The 'To Date' should be greater than 'From Date'.");
            //    return false;
            //}

            searchReport();

            return true;

        }

        function GetLastDayOfMonth(m, y) {

            var lastDay = 28;

            if (m == "04" || m == "06" || m == "09" || m == "11") {
                lastDay = 30;
            }
            else if (m == "02") {
                if ((y % 4) == 0) {
                    lastDay = 29;
                }
                else {
                    lastDay = 28;
                }
            }
            else {
                lastDay = 31;
            }

            return lastDay;
        }

        function searchReport() {
            // get the entered string
            var toSearch = $('payroll_report_table_filter input').val();

            $('#payroll_report_table').DataTable().search(toSearch);
            $('#payroll_report_table').DataTable().draw();
        }

        function initReportDataTable(identifier, url, columns) {
            var oTable = $(identifier).DataTable({
                "searchDelay": 2000,
                "scrollY": "1020px",
                "serverSide": true,
                "ajax": {
                    "type": "POST",
                    "url": url,
                    "contentType": 'application/json; charset=utf-8',
                    'data': function (data) {

                        var myVal = $('#month_year').val();
                        var dpVal = $('#department_id').val();
                        //console.log('444 ' + dtVal);
                        data.month_year = myVal;
                        data.department_id = dpVal;

                        return data = JSON.stringify(data);

                    }
                },
                "scrollX": true,
                "scrollCollapse": true,
                "processing": true,
                "paging": true,
                "deferRender": true,
                "columns": columns,
                "order": [0, "desc"],
                "iDisplayLength": 100,
                "aLengthMenu": [[10, 20, 100, 500, 1000], [10, 20, 100, 500, 1000]],
                "responsive": true

            });

            oTable.on("draw", function () {
                var keyword = $(identifier + '_filter > label:eq(0) > input').val();
                $(identifier).unmark();
                $(identifier).mark(keyword, {});

                $(identifier + ' tbody tr').click(function (e) {
                    $('tr.row_selected').removeClass('row_selected');
                    $(this).addClass('row_selected');
                });
            });


            // set the z-index of the 'processing...' overlay
            // to a great number, so that it may always display
            // on top of the table. Also set the text color
            // to black as it is white by default.
            $(identifier + '_processing').css({
                "z-index": "1000",
                "color": "rgb(0, 0, 0)"
            });


            // initiate search one second after the user
            // stops typing.
            function setSearchDelayOnDataTable(idSelector) {
                // remove all events from the search box
                $(idSelector + '_filter input').off();


                function customDataTableSearch() {

                    // get the entered string
                    var toSearch = $(idSelector + '_filter input').val();

                    // do nothing if the string is null.
                    // I've not tested this, it is just
                    // in case.
                    if (toSearch == null) {
                        return;
                    }

                    // initiate search programatically.
                    $(idSelector).DataTable().search(toSearch).draw();
                }

                // this variable holds the timer
                var timeout;

                // register a keypress on the search box
                $(idSelector + '_filter input').keypress(function () {
                    if (timeout != null) {
                        clearTimeout(timeout);
                        timeout = null;
                    }

                    // timeout set to 1 second.
                    timeout = setTimeout(customDataTableSearch, 1000);
                });

            }

            setSearchDelayOnDataTable(identifier);
        }

        function setTypeOfReportDropDown() {
            $('#type-of-report').chosen();
            $('#type-of-report').on("change", function () {
                switch ($('#type-of-report').val()) {
                    case "monthly-time-sheet":

                        $('#monthly-time-sheet').show();
                        $('#summary-report').hide();
                        $('#monthly-attendance-register').hide();

                        break;
                    case "summary-report":

                        $('#monthly-time-sheet').hide();
                        $('#summary-report').show();
                        $('#monthly-attendance-register').hide();

                        break;
                    case "monthly-attendance-register":

                        $('#monthly-time-sheet').hide();
                        $('#summary-report').hide();
                        $('#monthly-attendance-register').show();

                        break;
                }
            });
        }


        function setEmployeeDropDowns() {
            $('#monthly-time-sheet-employees').ajaxChosen({
                dataType: 'json',
                type: 'POST',
                url: '@Url.Action("ChangeEmployeePasswordDataHandler", "EmployeeManagement", null, Request.Url.Scheme)'
            }, {
                loadingImg: '@Href("~/Content/ChosenAutoComplete/loading.gif")'
            });
        }

        $(document).ready(function () {

            //$('#accordion').accordion("option", "active", 5);
            //$("input[type=submit], button").button();
            //$(".chzn-select").chosen();
            //$(".datepicker").datepicker();
            $(".datepicker").datepicker({ dateFormat: 'dd-mm-yy' });

            //setActiveLink("Monthly Time Sheet");
            HideMessage();
            //setEmployeeDropDowns();

            $('.hasDatepicker').keydown(function (e) {
                var unicode = e.keyCode ? e.keyCode : e.charCode;
                if (unicode != 13 && unicode != 8 && unicode != 9) { e.preventDefault(); }
            });

            $('.hasDatepicker').keydown(function (e) {
                var unicode = e.keyCode ? e.keyCode : e.charCode;
                if (unicode == 8) { e.preventDefault(); e.stopPropagation(); }
            });

            setTypeOfReportDropDown();

            //if ($('#pdf-no-data-found').text() != "") {
            //    console.log('msg ' + $('#pdf-no-data-found').text());
            //    $('#export_wait').hide();
            //}

            //DataTables.NET
            //search-payroll-report
            //generate-payroll-report

            //var fewSeconds = 5;
            //$('#search-payroll-report').click(function () {
            //    // Ajax request
            //    var btn = $(this);
            //    btn.prop('disabled', true); //btn.hide();
            //    setTimeout(function () {
            //        btn.prop('disabled', false); //btn.show();
            //    }, fewSeconds * 1000);

            //});


            //$('#search-payroll-report').button();
            $("#search-payroll-report").click(function (e) {

                if ($('#month_year').val() == "" || $('#month_year').val() == null) {

                    $('#alert_message').text("Please select a Month");
                    $('.bs-example-modal-sm').modal('show');

                    //alert('Please select a Month.');
                    return false;
                }

                if ($('#department_id').val() == "" || $('#department_id').val() == null) {

                    $('#alert_message').text("Please select a Department");
                    $('.bs-example-modal-sm').modal('show');

                    //alert('Please select a Month.');
                    return false;
                }

                //if ($('#monthly-time-sheet-employees').val() == "" || $('#monthly-time-sheet-employees').val() == null) {

                //    $('#alert_message').text("Please select an Employee Id");
                //    $('.bs-example-modal-sm').modal('show');

                //    //alert('Please select an Employee Id.');
                //    return false;
                //}

                //$('#pdf-no-data-found').val("");
                //$('#pdf-no-data-found').text("");

                submitcheck();
                //  serializeAndSend();
                e.preventDefault();

                //$('input[type="search"]').blur();

            });

            var url = '@Url.Action("PayrollReportByMonthDepartmentDataHandler", "Reports", null, Request.Url.Scheme)';

            var columns = [
                  { "data": "Id" },
                   { "data": "SalaryMonthYearText" },
                   { "data": "EmployeeCode" },
                   { "data": "EmployeeName" },
                   { "data": "GrossSalary" },
                   { "data": "TotalDeduction" },
                   { "data": "NetSalary" },
                   { "data": "actions" },
                   { "data": "CNIC" },
                   { "data": "BasicPay" },
                   { "data": "Increment" },
                   { "data": "Transport" },
                   { "data": "Mobile" },
                   { "data": "Medical" },
                   { "data": "CashAllowance" },
                   { "data": "Commission" },
                   { "data": "OvertimeInHours" },
                   { "data": "OvertimeAmount" },
                   { "data": "Food" },
                   { "data": "Night" },
                   { "data": "Rent" },
                   { "data": "GroupAllowance" },
                   { "data": "AnnualBonus" },
                   { "data": "LeavesCount" },
                   { "data": "LeavesEncash" },
                   { "data": "IncomeTax" },
                   { "data": "FineExtraAmount" },
                   { "data": "MobileDeduction" },
                   { "data": "AbsentsCount" },
                   { "data": "AbsentsAmount" },
                   { "data": "EOBIEmployee" },
                   { "data": "EOBIEmployer" },
                   { "data": "SESSIEmployee" },
                   { "data": "SESSIEmployer" },
                   { "data": "LoanInstallment" },
                   { "data": "OtherDeduction" },
                   { "data": "PaymentModeText" },
                   { "data": "BankNameText" },
                   { "data": "BankAccTitle" },
                   { "data": "BankAccNo" },
                   { "data": "NTNNumber" },
                   { "data": "EOBINumber" },
                   { "data": "SESSINumber" },
                   { "data": "AckStatusText" },
                   { "data": "PayStatusText" },
                   { "data": "UserHRComments" },
                   { "data": "AttachmentFilePath" },
                   { "data": "IsFirstMonthText" },
                   { "data": "PaymentDateTimeText" },
                   { "data": "CreateDateTimeText" },
                   { "data": "UpdateDateTimeText" }
            ];

            initReportDataTable('#payroll_report_table', url, columns);

        });

    </script>
}